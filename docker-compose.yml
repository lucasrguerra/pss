services:
  pss:
    build: .
    ports:
      - "${PORT:-3000}:80"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - wget
        - '-qO-'
        - 'http://127.0.0.1:3000/'
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
